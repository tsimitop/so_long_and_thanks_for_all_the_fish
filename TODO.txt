make sure that clicking the red cross button to  exit the window exits in a clean way
At every move, the current number of movements must be displayed in the shell.



0 for an empty space
1 for a wall
C for a collectible
E for a map exit
P for the player’s starting position

parsing
	OK If the map contains a duplicates characters (exit/start), you should display an error message.
	OK map only contains one exit (E)
	OK map only contains one starting position (P)

	OK map contains at least one collectible (C)

	OK map is rectangular

	OK only 2 arguments

	OK check for invalid file, existing, .ber file extension (last 4 characters .ber not .berr)

	OK check for empty file .ber

	OK check for invalid characters

	map is closed/surrounded by walls. If it’s not, the program must return an error

	check if there’s a valid path in the map
	coins and exit must be reachable (flood fill)
	no valid path (flood fill)
	no route to collectibles (flood fill)

	You must be able to parse any kind of map, as long as it respects the above rules

	If any misconfiguration of any kind is encountered in the file, the program must
	exit in a clean way, and return "Error\n" followed by an explicit error message of
	your choice.

	check for map dimensions?
